


dotnet publish -c Release -r linux-x64 --self-contained true
./bin/Release/net8.0/linux-x64/LinuxRestartTool


配置文件格式：



01:00       # 关闭时间 (HH:mm)
2.5         # 关闭间隔 (小时)
2023-01-01T00:00:00.0000000Z  # 最后关闭时间


管理命令：

手动终止进程：killall TerminalEntry.exe

查看日志：直接查看控制台输出

修改配置：编辑default_program.config后重启应用


[Unit]
Description=SC Server Monitor

[Service]
ExecStart=/path/to/monitor/LinuxRestartTool
Restart=always
User=scserver

[Install]
WantedBy=multi-user.target